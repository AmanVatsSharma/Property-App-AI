# Property-App-AI Mobile (Expo) — example env. Copy to .env in this directory.
# EXPO_PUBLIC_* are exposed to the app; never put secrets here.
EXPO_PUBLIC_API_URL=http://localhost:3333
EXPO_PUBLIC_GRAPHQL_HTTP=http://localhost:3333/graphql
