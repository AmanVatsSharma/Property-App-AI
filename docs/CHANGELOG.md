# Changelog

## [Unreleased]

### Added

- **Dev watch ENOSPC mitigation (2026-03-21)** — `api:compile:development` sets `CHOKIDAR_USEPOLLING` / `CHOKIDAR_INTERVAL` for `tsc-alias --watch` and `TSC_WATCHFILE=DynamicPriorityPolling` for `tsc --watch` to reduce Linux inotify pressure. `apps/api/README.md` documents `ENOSPC` and `sysctl fs.inotify.max_user_watches`.

- **API build & CI split (2026-03-21)** — `api:typecheck` runs `tsc --noEmit -p apps/api/tsconfig.app.json`; CI “Type check API” uses `nx run api:typecheck` instead of a cold `api:build --skip-nx-cache`. `api:compile` uses `tsc` + `tsc-alias` (replaces SWC) so path aliases work in emitted JS; `api:build` Webpack sets `skipTypeChecking: true` on NxAppWebpackPlugin to avoid ForkTsChecker OOM; Nx `outputs`/`cache` on `api:build` and `api:compile` for better caching. GraphQL JSON fields use `GraphQLJSON` from `graphql-type-json` only; removed duplicate `JsonScalar` Nest provider. Deleted unused `apps/api/src/shared/scalars/json.scalar.ts`.

- **API fast dev serve (2026-03-20)** — `api:serve` uses watch **`api:compile`** instead of a full Webpack build each start. **2026-03-21:** compile is **`tsc` + `tsc-alias`** (see **API build & CI split**); SWC-only dev compile was replaced for reliable `@api/*` in emitted JS. `tsconfig.app.json` app compile settings and root `compile:api` script remain relevant. Fixes retained: `UseGuards` from `@nestjs/common` (agent/broker resolvers); `JwtModule.registerAsync({ global: true })` in AppModule for NotificationGateway DI.

- **LLM token observability (2026-03-20)** — Parse LangChain `usage_metadata` / OpenAI-style usage; log structured fields (`llmFeature`, `llmInputTokens`, `llmOutputTokens`, `llmEstimatedUsdSonnet4Base` for Anthropic); Prometheus counter `llm_tokens_total{feature,provider,token_type}`. Wired in agent orchestrator (per-step accumulate), SearchParserService, AreaAssessorService, PriceForecastService. Docs: `docs/llm-token-billing.md`. MODULE_DOC updates (agent, area, search).

- **Root `pnpm dev:all`** — Runs API (`pnpm dev:api`) and web (`pnpm dev`) together via `concurrently` (named logs: api, web). Install deps after pull: `pnpm install`.

- **Phase 4 — Production-ready (2026-03-19)** — (1) **Next.js:** next.config full replacement — security headers (X-DNS-Prefetch, HSTS, CSP), rewrites for /api/*, /graphql, /health/* to backend, standalone output, image config (AVIF/WebP, remote patterns). (2) **PWA:** manifest.ts (typed route), sw.js (stale-while-revalidate, offline fallback), offline.html; layout preconnect for fonts and Unsplash, manifest URL /manifest.webmanifest. (3) **Cache:** CacheService.getOrSet and delByPrefix; PropertyService findAll list cache (60s) for public browse, bust on create/remove. (4) **Docker:** docker-compose.yml (postgres, redis, api, web with healthchecks); apps/api/Dockerfile and apps/web/Dockerfile (multi-stage, Nx/npm, libs/shared). (5) **CI:** .github/workflows/ci.yml — lint/type-check (api + web build), test-api (postgres + redis services), Docker build (no push). MODULE_DOC (property) and changelog updated.

- **Phase 3 — Mail, Admin Panel, Legal Checker (2026-03-19)** — (1) **Mail module:** MailService (Nodemailer SMTP, stub when SMTP_HOST/USER/PASS unset); MailModule @Global(); saved-search-alert HTML/text templates (dark-themed). (2) **Saved-search alerts:** runAlerts() sends email via MailService (fire-and-forget) alongside in-app notification; recipient placeholder until User has email. (3) **Env:** SMTP_* and NEXT_PUBLIC_SITE_URL in env.schema; .env.example SMTP section; nodemailer + @types/nodemailer in api. (4) **Admin panel:** /admin page and AdminClient (adminStats, users list with pagination, setUserRole); runGraphQL from shared; AdminGuard on GraphQL. (5) **Legal Checker:** /legal-checker replaced with real AI chat (LegalCheckerClient: gqlAskAgent with conversationHistory, quick prompts, AIThinkingChain, error/loading). MODULE_DOC (mail, saved-search) and changelog updated. i18n: new copy remains hardcoded; follow-up to externalise admin/legal-checker strings.

- **Phase 2 — Premium design system (2025-03-19)** — (1) **globals.css:** Phase 2 block with ease/shadow/radius tokens, full pc-* Property Card system (image wrap, scrim, frosted price tag, heart, badges, AI score, body), button/nav/card/search/sidebar/hero/detail/pagination micro-interactions, responsive and light overrides. (2) **PropertyCard:** Rewritten with Next/Image, glass price tag, SVG heart, AI Pick/New badges, AI score circle with teal/gold/coral by score, location/title/specs/ai-tip body; listingFor and createdAt for /mo and New badge. (3) **FilterSidebar:** New component (type, BHK, budget presets + custom min/max, Ready to Move, Verified only, AI Smart Match CTA). (4) **SearchPageClient:** Uses FilterSidebar; prop grid with auto-fill minmax(280px, 1fr); removed inline sidebar state. (5) **Landing:** Hero stats data-driven with LiveCounter prefix for Avg. Savings; "Families Helped" label. (6) **Nav:** Frosted glass pill container. Web MODULE_DOC changelog updated.

- **Saved search alert cron (2026-03-19)** — SavedSearchAlertScheduler added: @Cron(EVERY_DAY_AT_6AM) adds a job to saved-search-alerts queue so runAlerts() runs daily; ScheduleModule in AppModule; @nestjs/schedule dependency. MODULE_DOC (saved-search) updated.

- **Web footer Vedpragya credit line (2026-03-19)** — Footer now includes a professional credit line below the main bottom row: "Powered by Vedpragya" (link to vedpragya.com) · "Thoughtfully built for modern real estate". Copy externalised in `FOOTER_COPY` (copy.ts); new `.footer-credits` and `.footer-credits-link` styles; accessible link with `rel="noopener noreferrer"` and `data-testid="footer-vedpragya-link"`.

- **Phase 3 — AI features + UI polish (2026-03-19)** — (1) **Saved searches:** SavedSearchModule (migration CreateSavedSearch, entity, DTOs, repository, service, resolver, BullMQ processor SAVED_SEARCH_QUEUE); mySavedSearches, createSavedSearch, updateSavedSearch, deleteSavedSearch; runAlerts() with 24h cooldown, property search + notification create. (2) **Real-time notifications:** NotificationGateway (WebSocket namespace /notifications, JWT auth, pushToUser); NotificationService.create() pushes event to user room; main.ts IoAdapter; web useRealtimeNotifications hook, NotificationBell, socket.io-client. (3) **Price forecast:** PriceForecastService (agent module, LLM or defaults); GET /api/v1/price-forecast (NeighbourhoodController); AreaModule provides/exports PriceForecastService; agent getPriceForecastImpl wired to service; web price-forecast page live with PriceForecastClient. (4) **Broker dashboard:** myListings query (PropertyFilterDto.createdByUserId, repository filter); web /broker page and BrokerDashboardClient (stats, listings tab via gqlMyListings, enquiries, verification). (5) **Frontend:** Saved searches page and SavedSearchesClient; SaveSearchButton on search page; Nav: NotificationBell and Alerts link; graphql-client: saved search CRUD, gqlMyNotifications, gqlMarkAllNotificationsRead, gqlMyListings, AuthUser.role, QUERY_ME role. (6) **Dependencies:** @nestjs/websockets, @nestjs/platform-socket.io, socket.io (root); socket.io-client (web). MODULE_DOC (saved-search, notification) and CHANGELOG updated.

- **Phase 5 Testing (2026-03-18)** — Unit tests: (1) **PropertyService spec** updated with CacheService and MetricsService mocks, viewCount on mock entity, repository incrementViewCount and findPageWithFilters; ownership tests (update/remove throw ForbiddenException for non-owner non-admin, succeed for admin); changeStatus tests (ForbiddenException for non-owner, admin succeeds, ValidationError for invalid status); free listing gate (create throws ForbiddenException when existingListingCount > 0); findOne cache hit (returns cached property without calling repo or incrementViewCount). (2) **FavoriteService spec** (toggle add/remove, myFavorites, isFavorited). (3) **EnquiryService spec** (send with ownerUserId from property, send with no owner, myReceived, mySent). (4) **BrokerRequestService spec** (requestBrokerVerification create/return pending/throw when broker, approve setRole and updateStatus, reject updateStatus, NotFoundException). (5) **NotificationService spec** (create, myNotifications, markRead, markAllRead).

- **Phase 3 and Phase 4 API (2026-03-18)** — (1) **Broker verification:** BrokerModule (migration CreateBrokerRequest, BrokerRequest entity, repository, service, resolver); requestBrokerVerification(input?), reviewBrokerRequest(requestId, action, adminNote?) with AdminGuard; UserService.setRole on approve. (2) **Property view tracking:** migration AddPropertyViewCount (viewCount default 0); PropertyRepository.incrementViewCount; findOne fire-and-forget increment (only when loaded from DB; cache hit does not increment). (3) **Notification stub:** NotificationModule (migration CreateNotification, entity, repository, service, resolver); create, myNotifications(limit?, offset?), markNotificationRead(id), markAllNotificationsRead; EnquiryService.send creates notification for listing owner (type 'enquiry'). (4) **Redis cache:** CacheService (get/set/del, Redis-backed); PropertyService findOne read-through cache (300s TTL), invalidate on update/remove; view count increment only on DB path. (5) **Cursor pagination:** PropertyFilterDto.after (ISO cursor); PropertiesPage (items, nextCursor, total); findPageWithFilters in repository; findAllPage in service; propertiesPage(filter) query. (6) **Prometheus metrics:** MetricsService (agent_calls_total, agent_duration_seconds, otp_sent_total, property_created_total); wired in AgentOrchestratorService, AuthService.sendOtp, PropertyService.create. (7) **Swagger:** @nestjs/swagger; DocumentBuilder in main.ts, SwaggerModule.setup('api/docs'); SearchRequestBody DTO with @ApiProperty; NeighbourhoodQueryDto @ApiProperty; upload controller @ApiResponse(200/400).

- **Phase 2 core features (2026-03-18)** — (1) **Favorites:** FavoriteModule (migration CreateFavorite, entity, repository, service, resolver); toggleFavorite(propertyId), myFavorites; web: PropertyDetailActions (Save, Enquire, Status), /favorites page; mobile: toggleFavorite, sendEnquiry, property detail Save/Enquire. (2) **Enquiry:** EnquiryModule (migration CreateEnquiry, entity, CreateEnquiryInput, repository, service, resolver); sendEnquiry(input), myReceivedEnquiries, mySentEnquiries; web/mobile enquiry form on property detail. (3) **Agent conversation persistence:** Migration CreateAgentConversation; AgentConversation entity (JSON messages via JsonScalar); ConversationService (startConversation, appendMessages, getConversation, myConversations); askAgent(conversationId?) loads/saves conversation, AskAgentResult.conversationId; myAgentConversations query; graphql-type-json + JsonScalar. (4) **Property status:** changeStatus in PropertyService (draft/active/sold/rented), changePropertyStatus mutation; migration AddPropertyStatus (default 'active'); web PropertyDetailActions status dropdown for owner. (5) **Free listing gate:** create() throws ForbiddenException when existingListingCount > 0; web PostPropertyForm and mobile post screen show "Free listing limit reached. Please upgrade to post more listings." on 403.

- **API Phase 1 critical fixes (2026-03-18)** — (1) **Property ownership:** update and delete restricted to owner or admin; PropertyService.update/remove take requestingUserId and requestingUserRole; assertOwnerOrAdmin enforces; resolver passes ctx.req.user (sub, role); ForbiddenException when non-owner. (2) **Redis-backed OTP store:** OtpStoreService (Redis when REDIS_URL set, in-memory fallback); OtpService delegates to OtpStoreService; set/verify/get async; AuthService awaits. RateLimitModule exports REDIS_THROTTLE_TOKEN. (3) **Migration + indexes:** AddCreatedByUserIdAndIndexes (createdByUserId column if missing; indexes: property location GIN, price, bedrooms, areaId, createdByUserId, type, createdAt, area localityNormalized+cityNormalized, user phone). (4) **AgentQueueService:** AGENT_QUEUE_NAME is static readonly on class. (5) **Logger:** LoggerService gains info() and trace(); agent-tools warn calls use (message, meta) order. (6) **Area score filters:** property repository area JOIN uses IS NOT NULL on schoolsScore/connectivityScore when filtering.

- **Enterprise production readiness (2026-03-17)** — (1) **Security:** Production requires `JWT_SECRET` (min 16) and explicit `CORS_ORIGIN` (no `*`); env.schema validates accordingly; Redis-backed ThrottlerStorage and AgentRateLimitStore when `REDIS_URL` set (RateLimitModule, RedisThrottlerStorage, RedisThrottleShutdown, AgentRateLimitStore). (2) **Resilience:** `REQUEST_TIMEOUT_MS` configurable; TimeoutInterceptor uses it via factory; `withRetry()` for GeocodingService, NearbyService, AreaAssessorService (2 retries, exponential backoff on 5xx/network); GraphQL formatError consistent shape, no stack in production. (3) **Health:** `GET /health/live` (liveness), `GET /health/ready` (readiness: DB + Redis), `GET /health` legacy. (4) **Data:** TypeORM pool `DB_POOL_MAX`, `DB_POOL_IDLE_TIMEOUT_MS`; CI migrations job (PostgreSQL service, `nx run api:migration:run`); DEPLOYMENT backup and migration-before-deploy notes. (5) **Observability:** `GET /metrics` (Prometheus, default Node metrics); DEPLOYMENT log/metrics and no-PII note. (6) **CI/CD:** API tests with `--coverage` and coverageThreshold 40% global; CD and secrets note in DEPLOYMENT. (7) **Docs:** RUNBOOK.md (health, logs, DB, migrations, rate limits, graceful shutdown, escalation); DEPLOYMENT env contract and production requirements.

- **AI Search and Listing Enrichment (2026-03-17)** — (1) **Property–area link:** Property entity and DTOs now have areaId, locality, city; migration AddPropertyAreaLink; PropertyService resolves area on create/update (GeocodingService reverseGeocode + AreaService.getOrCreate). (2) **Search by area:** PropertyFilterDto extended with schoolsScoreMin, connectivityScoreMin; repository inner-joins Area when set. Agent search_properties tool and domain prompt: pass schools_score_min/connectivity_score_min for "near school"/"near metro". (3) **NL search:** SearchModule (SearchParserService + prompt); POST /api/v1/search (public); GraphQL searchPropertiesByQuery(query); web search box runs one-shot NL search and shows results, "Refine with AI" opens Fab. (4) **Listing enrichment:** nearbyAmenities (JSONB), migration AddPropertyNearbyAmenities; NearbyService (Mapbox proximity for metro, school, hospital) on create/update. (5) GeocodingService: reverseGeocode(lat, lng) and locality/city in GeocodeResult. MODULE_DOC (property, search), ai-features-inventory updated. AI text/image analysis documented as planned.

- **MVP audit (2026-03-17)** — Backend area, auth, and agent modules verified; web API client sends `X-API-Version`, neighbourhood page uses real API with loading/error/connect-API states; legal-checker and price-forecast show "Coming soon" CTA (no mock); landing and search AI positioning documented; tests and docs (api-web-sync-report, DEPLOYMENT.md MVP checklist, ai-features-inventory, CHANGELOG) updated.

- **Web: neighbourhood, price-forecast, legal-checker (2026-03-17)** — (1) **Neighbourhood:** Replaced hardcoded scores with real API: client component `NeighbourhoodExplorerClient` fetches GET /api/v1/neighbourhood?locality=...&city=... via NEXT_PUBLIC_API_URL; loading and error states; when API unavailable shows "Connect API" (no mock numbers). (2) **Price-forecast:** Removed hardcoded price table; single "Coming soon" message that forecast will use real data when API is ready. (3) **Legal-checker:** Kept UI; added prominent "Coming soon — RERA verification will be available soon"; search input/button disabled and read-only; no fake results. File headers, design tokens, data-testid and accessibility preserved.
- **MVP readiness audit (2026-03-17)** — Subagent-driven audit; mock data audit report; API–web sync report; AI features inventory; neighbourhood API wired from web; price-forecast and legal-checker show Coming soon (no mock data); MVP checklist and test report. Platform positioned for AI-powered search and ease with AI; deploy checklist in DEPLOYMENT.md.

- **Backend MVP-ready (2026-03-17)** — No mock listing or user data in production (property, auth, agent, area, admin, storage). GET /api/v1/neighbourhood registered (NeighbourhoodController, AreaModule in AppModule). Agent tools search_properties, create_listing, score_property live; get_price_forecast, check_rera, analyze_document, get_negotiation_advice return "Coming soon".

- **Neighbourhood score HTTP API** — GET /api/v1/neighbourhood?locality=...&city=... (public). Area module: NeighbourhoodController, NeighbourhoodQueryDto; returns locality, city, livabilityScore, connectivityScore, schoolsScore, safetyScore, priceTrendPctAnnual, amenitiesSummary, lastAssessedAt. Uses AreaService.getOrCreate with assessIfMissing: true (no mock data). MODULE_DOC (area) updated. (2026-03-15)

- **Legal-checker & price-forecast API proposal (doc only)** — New [docs/architecture-legal-forecast-api.md](architecture-legal-forecast-api.md): minimal API surface for legal-checker (e.g. POST /api/v1/legal/check-rera with projectNameOrNumber) and price-forecast (GET /api/v1/forecast?locality=...&city=...&horizonMonths=24); response shape with status "coming_soon" vs "ok" so frontend can show proper UI; no implementation. Aligns with agent tools check_rera and get_price_forecast (currently placeholders). (2026-03-15)

- **AI search filters, result display, and thinking-chain UI (2025-03-15)** — (1) **Agent:** search_properties tool extended with min_price, type, sort_by, sort_order; searchPropertiesImpl returns ToolResult with content and sources (one per property with id) so the web client can show property cards. (2) **Web:** New AIThinkingChain component (apps/web/src/components/agent/AIThinkingChain.tsx) shows animated "Understanding your query…", "Searching properties…", "Analyzing results…" steps while askAgent is loading. (3) **Web:** AIPropertyCards component fetches and renders compact property cards from agent response sources (type 'property' with id) in the AI Fab panel. (4) **Web:** Search page AI search input and "Search" button open the AI Fab with prefilled prompt; "✦ AI Smart Match" opens the Fab with a prompt built from current filters (city, BHK, type, budget). MODULE_DOC (agent) updated with search_properties tool contract and changelog.

- **Gitignore coverage: Playwright and Turbo** — Root `.gitignore`: added `playwright-report/`, `test-results/` under Testing and `.turbo/` under Optional. `apps/web/.gitignore`: added `playwright-report/`, `test-results/` and `!.env.example` so committed example env is allowed. No new .gitignore in api/admin/api-e2e (root covers dist, node_modules, coverage). (2026-03-15)

- **Web e2e and API unit test fixes (2026-03-15)** — (1) **Web e2e** (`apps/web/e2e/search-and-property.spec.ts`): Search tests made resilient to empty API (assert search-top-bar, sort-select, listings-wrap, results-meta); click-card test asserts property/[id] when cards exist else stays on /search; AI Fab tests scoped to dialog (getByPlaceholder/getByText within dialog) and use Send button (not Ask). All 5 e2e tests pass with API-sourced landing/featured and search → property/[id] flow; no /property/detail or static slug. (2) **API unit tests**: Jest moduleNameMapper stubs for @langchain (core/tools, core/messages, openai, anthropic) so agent-tools and agent-orchestrator specs run without ESM; agent-orchestrator mock includes setAgentContext/clearAgentContext; agent-tools scoreAndPersistProperty assertion uses toMatchObject for PropertyNotFoundError. Auth, property, agent, admin, guards specs: 39 tests pass. (3) **Mock data**: No mock listing data in production code paths; fixtures/mocks only in test files (e2e uses real API; unit tests use in-spec mocks).

- **MVP readiness pass (2026-03-15)** — Deployment and docs aligned for production MVP: (1) **Migrations:** exact run command documented in [docs/DEPLOYMENT.md](DEPLOYMENT.md): `nx run api:migration:run` from repo root (script: `apps/api/src/database/run-migrations.ts`, env: DB_*). (2) **Web env:** web app env path set to [apps/web/.env.example](apps/web/.env.example); copy to `apps/web/.env.local` or create from root `.env.example` if missing. (3) **MVP checklist** in DEPLOYMENT.md tightened: API `DB_*` and `JWT_SECRET` set, migrations applied, `SMS_PROVIDER=twilio` or `msg91` with credentials for production OTP, web `NEXT_PUBLIC_GRAPHQL_HTTP` or `NEXT_PUBLIC_API_URL` set, no mock listing or featured data; landing featured and links API-sourced or fixed. (4) MODULE_DOC changelogs (admin, user, storage, health, and existing auth, area, agent, property) made consistent with 2026-03-15 MVP readiness. No mock data in any app; empty states when API unavailable or empty.

- **API runnable migration script** — `apps/api/src/database/data-source.ts` (TypeORM DataSource with DB_* env and migrations path); `apps/api/src/database/run-migrations.ts` loads dotenv (root .env then apps/api/.env, same order as ConfigModule) and runs `runMigrations()`. Nx target `migration:run` runs the script from `apps/api` via `node -r @swc-node/register src/database/run-migrations.ts`. README and DEPLOYMENT.md document the exact command: `nx run api:migration:run`. (2025-03-15)

- **Auth MVP readiness: production checklist for SMS/OTP** — MODULE_DOC (auth) now includes an explicit "MVP / Production checklist" section: for deployable MVP with real OTP, set SMS_PROVIDER to twilio or msg91 with corresponding credentials (Twilio: TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, TWILIO_FROM; MSG91: MSG91_AUTH_KEY). When unset or stub, OTP is only logged—no SMS sent. (2026-03-15)

- **Area module MVP readiness** — Fallback scores (when LLM missing/fails) documented; no mock listing data. (2026-03-15)

- **Documentation consistency pass** — All MODULE_DOC.md files aligned to canonical template (Short, Purpose, Files, Dependencies, APIs, Env vars, Change-log). READMEs (root, api, web, admin, mobile) updated for consistent structure and .env.example pointers. [docs/DEPLOYMENT.md](DEPLOYMENT.md) and [EXTERNAL_DATA_SOURCES.md](EXTERNAL_DATA_SOURCES.md) verified. See plan docs_consistency_subagents.

- **API test result summary** — `docs/TEST_RESULT_SUMMARY.md`: API unit tests (5 passed, 2 agent suites failed due to Jest/ESM with @langchain/p-retry); API e2e not run (ts-node missing for jest.config.ts); coverage for property/auth/admin critical paths; suggested additions: PropertyResolver and AuthResolver unit tests.

- **MVP readiness: mobile mock data removed; property detail not-found state** — (1) Mobile: mock/fallback listing and featured data removed; property list and home featured section use API only, with empty state when unconfigured or no results. (2) Property detail: not-found state when property ID is missing or API returns null (web and mobile). (3) Related audit updates: API modules (no mock listing in prod, env contract, MODULE_DOC MVP notes); web (GraphQL-only listing/detail, DEMO_IMAGES fallback/marketing only, type sync with API); admin (dashboard/properties/users from API only, no mock data). Ensures deployable MVP with no mock data; see docs/DEPLOYMENT.md checklist.

- **API modules MVP readiness audit** — (1) No mock listing/user data in production code (mocks only in __tests__/*.spec.ts). (2) Env contract: added `SMS_PROVIDER` to `env.schema.ts` (stub | twilio | msg91); JWT_SECRET, DB_*, OPENAI_API_KEY, ANTHROPIC_API_KEY already in schema; .env.example documents SMS and AI keys. (3) MODULE_DOC.md: added MVP/deploy notes to property, auth, admin, agent, user, storage, health, area. Doc/config only; no test mocks changed.

- **Web: GraphQL/API type sync and no-mock clarity** — (1) No mock listing data: listing and detail in apps/web come from GraphQL only (gqlProperties, getPropertyById → gqlProperty). (2) DEMO_IMAGES used only as fallback when API returns no image and for marketing/landing; README updated to state this. (3) ApiProperty and PropertyFilter in apps/web aligned with API: ApiProperty now includes createdByUserId and isFreeListing; PropertyFilter sortOrder allows "ASC"|"DESC"; PROPERTY_FIELDS query requests the new entity fields; JSDoc references API property entity and PropertyFilterDto.

- **Mobile APK build: copy to known location and docs** — (1) `apps/mobile/scripts/copy-apk.js` copies the debug APK to `apps/mobile/app-debug.apk` after a successful build. (2) Nx target `build-apk` runs the copy script after `assembleDebug`. (3) Root `.gitignore` ignores `**/app-debug.apk`. (4) `apps/mobile/README.md` Build APK section updated with prerequisites table, copy output location, `adb install` example, and summary table.
- **Per-app env and .env.example** — (1) Each app has its own `.env.example`: `apps/api/.env.example`, `apps/web/.env.example`, `apps/admin/.env.example`, `apps/mobile/.env.example`; copy to `.env` or `.env.local` in that directory. (2) Root `.env.example` kept as full-stack reference with a note pointing to per-app examples. (3) API `ConfigModule` loads `apps/api/.env` first, then root `.env` (app-level overrides root). (4) README and app READMEs updated for per-app env; DEPLOYMENT.md references root and per-app examples.

- **Mobile: light theme, header with location and theme toggle** — (1) **Theme:** ThemeProvider with AsyncStorage (urbannest-theme) and NativeWind colorScheme.set(); UrbanNestLightTheme; StatusBar and nav/tab bar theme-aware; tailwind darkMode: 'class' and light palette (light-night, light-dark, light-teal, etc.). All tab and stack screens use useTheme() and conditional classes for light/dark. (2) **Header:** AppHeader on tab screens: left = location (tap opens LocationSheet); right = avatar (tap opens menu with Theme: Light/Dark). (3) **Location:** LocationProvider and LocationSheet with “Use your current location” (expo-location, foreground permission) and city list (CITIES in constants/cities.ts). Search (and fetchProperties) filter by location (city string or coords bounding box). app.json: iOS NSLocationWhenInUseUsageDescription, Android ACCESS_COARSE_LOCATION/ACCESS_FINE_LOCATION. (4) **Docs:** mobile MODULE_DOC updated.
- **Mobile app / desktop prompt (web)** — When the web app is opened on a mobile viewport (max-width 768px), a dismissible banner appears at the top of the main content inviting users to download the app (App Store / Google Play) or continue on the device. Copy is i18n-ready via `apps/web/src/lib/copy.ts` (MOBILE_PROMPT_COPY); dismissal is stored in sessionStorage for the session. App store URLs use `NEXT_PUBLIC_APP_STORE_URL` and `NEXT_PUBLIC_PLAY_STORE_URL` when set. Component: `MobileAppPrompt.tsx`; styles use design tokens and 44px touch targets.
- **MVP readiness: upload controller and upload auth** — (1) **API:** Implemented `upload.controller.ts` (was empty): `POST /api/v1/upload` (single file, field `file`) and `POST /api/v1/upload-multiple` (field `files`, max 10); uses `StorageService.uploadFile`; protected by global AuthGuard when `JWT_SECRET` set. (2) **Web:** `upload-api.ts` accepts optional `UploadImageOptions.token` and sends `Authorization: Bearer <token>` when provided; PostPropertyForm passes `token` to `uploadImage(file, { token })` so uploads work when signed in. (3) **Docs:** storage MODULE_DOC and DEPLOYMENT.md updated with upload auth behaviour and checklist.
- **MVP readiness: no mock data** — (1) **Web property detail:** Removed `MOCK_PROPERTIES`; `getPropertyById` returns null when GraphQL URL missing or API fails; property page shows 404. (2) **Web search:** Removed hardcoded `PROPERTIES`; loading state then API list or empty state; results count from API. (3) **Mobile search:** Removed `FALLBACK_PROPERTIES`; empty state when API unconfigured or returns empty. (4) **Web post property:** No mock success; backend URL required; submit disabled and banner shown when not configured. (5) **API OTP:** `SmsService` added with Twilio and MSG91; set `SMS_PROVIDER=twilio` or `msg91` and credentials for production OTP. (6) **Agent tools:** Price forecast, RERA, document analysis, negotiation return "Coming soon" instead of placeholder text. (7) **Area assessor:** MODULE_DOC documents fallback scores when LLM absent. (8) **Demo images:** Used only for marketing/landing and as fallback when a real property has no image; `defaultPropertyCover` for search and detail. (9) **Deployment:** `docs/DEPLOYMENT.md`, root `Dockerfile` for API, `.env.example` SMS vars.
- **Dependency fix for npm install** — apps/mobile: `react-native-maps` version changed from `1.20.4` (nonexistent) to `^1.20.1` so `npm install --legacy-peer-deps` succeeds at repo root.
- **Web build fixes** — (1) apps/web tsconfig: added explicit paths for `@property-app-ai/shared` and `@property-app-ai/shared/*` pointing to `../../libs/shared/src/` so Next.js build resolves the shared lib. (2) apps/web graphql-client: fixed getGraphQLUrl() so the “never nullish” TypeScript error is resolved (use fromApiUrl() helper and proper fallbacks).
- **Optimize-structure follow-ups** — (1) **refactor-scan.json:** Updated testOk/testNote for API test target; lintOk/lintNote set to true after lint fixes. (2) **Web lint:** Input.tsx uses useId() instead of Math.random(); Nav, AuthProvider, SearchPageClient avoid synchronous setState in effects (queueMicrotask); AIFab unused import removed. (3) **Admin lint:** apps/admin/eslint.config.mjs added (minimal flat config with globalIgnores) so admin:lint runs. (4) **CI:** Madge cycle check (npx madge --circular apps/api/src) and API unit tests (nx run api:test) added to .github/workflows/ci.yml; madge added to root devDependencies. (5) **API Jest:** apps/api/jest.config.js moduleDirectories includes workspace root so ts-jest resolves when run from apps/api.
- **Enterprise src structure refactor (Phase 1)** — API path aliases and refactor tooling. (1) **Path aliases:** `@api/shared/*`, `@api/common/*`, `@api/modules/*`, `@api/app/*`, `@api/database/*` in `apps/api/tsconfig.app.json` and webpack resolve in `apps/api/webpack.config.js`. (2) **Imports:** All cross-folder relative imports in `apps/api/src` replaced with `@api/*`; within-module relative imports unchanged. (3) **Tooling:** `refactor-scan.json` (baseline file/module counts, entry points, circular deps); `migration-map.json`; `scripts/refactor-verify.sh` and npm script `refactor:verify` (lint, api/web build, API tests, madge cycle check); `refactor-PR.md` template. (4) **API test target:** `test` target in `apps/api/project.json`; `apps/api/jest.config.js` with `moduleNameMapper` for `@api/*`; jest and ts-jest added to root devDependencies. No renames of `modules/` or `common/errors`. See refactor-PR.md.
- **Six new Cursor rules** — (1) **naming-conventions.mdc:** Single source for file (kebab-case), component (PascalCase), hook (useXxx), env (UPPER_SNAKE), constants, API naming. (2) **scalability-and-vision.mdc:** Design for scale and global product — stateless services, shared libs, feature flags, i18n-ready, multi-tenant, document scaling in MODULE_DOC. (3) **testing.mdc:** Use tester subagent; tests next to code (*.spec.ts); cover critical paths and e2e for key flows. (4) **security.mdc:** No secrets in code (use env); validate/sanitize inputs; use existing guards. (5) **mobile-expo.mdc:** File-scoped to apps/mobile — NativeWind, safe area, platform-specific behaviour, shared types from libs. (6) **i18n.mdc:** Externalise copy, key-based translations, no hardcoded user-facing strings; global-product ready. master.mdc FILE NAMING section points to naming-conventions.mdc for full naming rules.
- **Design principles rule (design-principles.mdc)** — Always-on rule so the agent can check and reason about UI/UX quality: visual hierarchy, consistency, clarity, whitespace and rhythm, feedback and affordance, progressive disclosure, and accessibility (contrast, touch targets, focus). Includes a "before shipping" checklist. frontend-always.mdc updated to point to design-principles when evaluating or improving a screen.
- **Property-App-AI conventions skill** — Project skill at `.cursor/skills/property-app-conventions/` (SKILL.md + reference.md) for consistent application of repo conventions: module structure, file headers, Pino logging, DTOs, MODULE_DOC and changelog, PR checklist, subagent delegation. Use when editing or adding code or when the user asks for project standards.
- **User roles: enterprise RBAC (user, broker, admin)** — (1) **Roles:** UserRole extended with `BROKER`; `user` (default), `broker`, `admin`. (2) **Assignment:** Admin via `ADMIN_PHONES`; broker via `BROKER_PHONES` env and/or admin-only mutation `setUserRole(userId, role)`. AuthService.verifyOtp applies broker when phone in BROKER_PHONES (admin takes precedence). (3) **Guards:** `@Roles(...roles)` decorator and `RolesGuard` (reflector-based); AdminGuard refactored to use shared `requireRoles`/`getRequestFromContext` from roles.guard. (4) **Admin:** `setUserRole` mutation in AdminResolver; returns User, throws NotFoundException if user not found. (5) **Optional RBAC:** `common/rbac/permissions.ts` (PERMISSIONS constants), `role-permissions.ts` (ROLE_PERMISSIONS map, roleHasPermission). User and Admin MODULE_DOC updated. See User MODULE_DOC and plan user_roles_enterprise_rbac.
- **Admin panel (Next.js app + API)** — (1) **Backend:** User entity `role` (user | admin), migration AddUserRole; env `ADMIN_PHONES` (comma-separated) to assign admin on OTP login; JWT payload includes `role`; AdminGuard (ForbiddenException if role !== admin); AuthService.verifyOtp sets role from allowlist and returns role in user; UserService.setRole, UserRepository.updateRole/findAll/count; Admin module with users(limit, offset) and adminStats (propertyCount, userCount) protected by AdminGuard; PropertyService.getCount, PropertyRepository.count. (2) **Admin app:** New Next.js app at `apps/admin`: login (OTP, role check), protected dashboard layout with sidebar (Dashboard, Properties, Users, Log out); dashboard (stats cards + recent properties); properties list (table, pagination), create, edit, delete; users list (table, pagination). Root scripts: `dev:admin`, `build:admin`. See [apps/admin/README.md](../apps/admin/README.md) and [apps/api/src/modules/admin/MODULE_DOC.md](../apps/api/src/modules/admin/MODULE_DOC.md).
- **Property rating score tools and region persistence** — (1) **Area module:** New `area` entity (locality, city, normalized keys, livabilityScore, connectivityScore, schoolsScore, safetyScore, priceTrendPctAnnual, amenitiesSummary, dataSource, lastAssessedAt); migration CreateArea; AreaRepository (findByLocalityAndCity, create, update); AreaService.getOrCreate(locality, city, { assessIfMissing }). (2) **Area assessor:** AreaAssessorService with LLM-based assessment (structured JSON output), TTL (AREA_ASSESSMENT_TTL_DAYS), needsAssessment; area-assess prompt. (3) **External data:** docs/EXTERNAL_DATA_SOURCES.md (NHB HPI, PropIndex, RERA, Mapbox/Places, provider interface); AreaDataProvider interface; env AREA_PROVIDER, AREA_ASSESSMENT_TTL_DAYS. (4) **Agent tools:** get_neighbourhood_score and new assess_region use AreaService.getOrCreate with assessIfMissing; score_property and scoreAndPersistProperty resolve property location to Area, compute score from area + property (rule-based), persist aiScore/aiTip. Domain prompt: assess_region then score_property; recommend AGENT_THINKING_BUDGET_TOKENS and AGENT_PLAN_FIRST for scoring. See [apps/api/src/modules/area/MODULE_DOC.md](../apps/api/src/modules/area/MODULE_DOC.md) and [docs/EXTERNAL_DATA_SOURCES.md](EXTERNAL_DATA_SOURCES.md).
- **Property-to-maps integration** — (1) **Backend:** Property entity and DTOs now include `latitude` and `longitude` (nullable). Migration `AddPropertyLatLng` adds columns. `GeocodingService` (Mapbox) geocodes address to lat/lng on create/update when client does not send coordinates; optional env `MAPBOX_ACCESS_TOKEN`. Properties query supports map viewport bounds (`minLat`, `maxLat`, `minLng`, `maxLng`) for efficient map loading. (2) **Web:** Leaflet + OpenStreetMap map in search page; "Map" view shows properties with coordinates as markers; click marker popup links to property detail. Search fetches from GraphQL and uses API list for both grid and map. (3) **Mobile:** `react-native-maps`; search tab has List/Map toggle; map view shows markers for properties with lat/lng; tap callout navigates to property detail. See property MODULE_DOC.md.
- **AI-first listing UX polish (traditional form kept)** — (1) **Post property page:** New CTA block above the form: “Or describe your property in your own words — our AI will create the listing for you” with “Describe & post with AI →” opening the AI Fab with prefilled prompt “I want to post a listing”; AIFabProvider context (open with prefill) and PostPropertyAICta client component. (2) **AI panel:** Stronger empty state with example line: “Try: ‘I want to list my 2BHK in Koramangala for rent at ₹25,000’ — the AI will create the listing for you”; create_listing tool “View listing” suggested action now points to `/property/[id]` (property detail page). (3) **Landing:** One AI-first line and “Try AI” CTA in hero: “Describe what you want in plain language — search properties or post your own listing. Our AI handles the rest.” (4) **Profile dropdown:** Nav fetches `me` when authenticated and shows “Signed in as {displayName}” or “Signed in as ******3210” (masked phone). (5) **Optional:** Typing-style effect for assistant messages in AI Fab (chunk reveal over ~40 ms intervals). All changes additive; existing form and traditional flows unchanged.
- **Web auth, user profile, listing ownership, and AI agent session** — (1) **Backend:** User entity (id, phone unique, displayName); UserService getOrCreateByPhone; verifyOtp get-or-creates User and returns token + user; JWT sub is user.id. UserResolver: me, updateMyProfile (auth required). Property: createdByUserId on entity; createProperty sets it from req.user. Agent: userId passed from resolver to orchestrator and queue; create_listing tool (title, location, price, type, listing_for, etc.) calls PropertyService.create with createdByUserId; AskAgentInput.conversationHistory for multi-turn; domain prompt updated for posting listings. (2) **Web:** VerifyOtpResult includes user; gqlMe, gqlUpdateProfile; AuthProvider openLoginModal/setOpenLoginModal; Nav Profile dropdown and Sign In opens modal; PostPropertyForm on 401 opens login modal and shows "Sign in to post a listing." AIFab: multi-turn chat UI, sends conversationHistory and auth header, "Talk to UrbanNest AI" and create listing via agent when signed in. MODULE_DOC and property/agent/auth/user docs updated.
- **S3 image upload (backend + frontend)** — (1) **Backend:** New `storage` module with `StorageService` (AWS S3 upload, image type/size validation), REST `POST /api/v1/upload` (single file) and `POST /api/v1/upload-multiple` (multiple files); env: `AWS_REGION`, `S3_BUCKET`, `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `S3_PUBLIC_BASE_URL` (all optional). (2) **Property:** Entity and DTOs extended with `coverImageUrl` and `imageUrls` (JSONB); migration `AddPropertyImageUrls`; repository create/update persist image URLs. (3) **Web:** `upload-api.ts` for `uploadImage(file)`; PostPropertyForm file input (multiple, JPEG/PNG/WebP, 10 MB max), uploads then `gqlCreateProperty` with `coverImageUrl`/`imageUrls`; GraphQL queries and `ApiProperty` include image fields; `property-api` maps to `coverImage`/`galleryImages`; `next.config` remotePatterns for S3. See [apps/api/src/modules/storage/MODULE_DOC.md](../apps/api/src/modules/storage/MODULE_DOC.md) and property MODULE_DOC.
- **Auth (mobile + OTP) and create-listing sync** — (1) **Backend:** New `auth` module with `sendOtp(phone)` and `verifyOtp(phone, code)` GraphQL mutations (both `@Public()`); in-memory OTP store with 5-min TTL; JWT issued on verify; Indian mobile validation (10 digits, 6–9 start). (2) **Web:** GraphQL `createProperty` in client; PostPropertyForm extended with title and price, submits via `gqlCreateProperty` with correct DTO; AuthProvider + LoginModal (phone → send OTP → code → verify); token in localStorage; Nav Sign In opens modal, Sign out clears token; create listing sends Bearer when signed in; 401/Unauthorized shows "Sign in to post". (3) **Mobile:** `sendOtp`/`verifyOtp` in GraphQL client; SecureStore token via `lib/auth-store.ts`; login screen at `/login` (phone → OTP → verify, then replace to tabs); More → Sign in navigates to login; Post screen form submits via `createProperty` with `getAuthHeaders()`; 401 shows "Sign in to post". (4) **Optional:** `createProperty` remains protected by global AuthGuard when `JWT_SECRET` is set; no `@Public()` on createProperty.
- **Plan 2 (deployable robust):** `.env.example` with all API, web, and mobile vars; README Environment and Production/security and Resiliency/operations and Deployment and Deploy checklist; API e2e updated (GET /, GET /health, GraphQL properties); unit test for AgentRateLimitGuard; production JWT_SECRET warning in main.ts; web e2e for AI Fab (open panel, submit prompt); README Run tests; GitHub Actions CI (lint, build api/web, web unit tests); mobile README Environment (EXPO_PUBLIC_*); mobile search shows "Showing demo data (server unavailable)" when API fails.
- **Stack evaluation implementation (enterprise scalability)** — (1) **Unify clients:** Shared GraphQL client in `libs/shared` (runGraphQL); web and mobile use `@property-app-ai/shared` for NAV_LINKS, FOOTER_LINKS, SEARCH_TABS; web property-api uses GraphQL for getPropertyById; mobile API client (`lib/graphql-client`) and search/property detail fetch from API with fallback mock. (2) **Expose AI:** Web AI Fab opens panel and calls `askAgent` GraphQL mutation; displays answer, sources, suggested actions. (3) **Agent scalable:** BullMQ queue when `AGENT_QUEUE_ENABLED` and `REDIS_URL`; `askAgent` returns union (AskAgentResult | AskAgentAsyncResult with jobId); `agentJobStatus(jobId)` for polling; AgentRateLimitGuard (AGENT_RATE_LIMIT_PER_MIN). (4) **API versioning + auth:** Header versioning (`X-API-Version`, default 1); JWT validation in AuthGuard when `JWT_SECRET` set; @Public() for root and health; request logs include userId when present. (5) **Observability + docs:** Agent orchestrator logs input size, steps, durationMs, provider, model; Redis health indicator when REDIS_URL set; README "Run full stack" (web + API + DB, optional Redis) and dev:api/build:api scripts.
- **Frontend: mobile responsive layout (apps/web)** — Viewport meta (device-width, initialScale, maximumScale) in layout; mobile-first responsive breakpoints in globals.css (max-width: 1023px, max-width: 639px). Nav: hamburger + slide-in drawer below 1024px with all links, theme toggle, Sign In, Post Free; desktop pills/right visible from 1024px. Responsive padding (52px → 16px on small screens), section/footer/hero/search/detail/EMI/post-property/about grids single- or two-column on mobile. Search: single-column layout, filters above results, top bar stacks (query full width, then meta/sort/view). Touch: min 44px tap targets for nav, buttons, filters, pagination; AI FAB safe-area-inset-bottom. Tap highlight suppressed. Test at 320px–1024px.
- **Mobile: Android APK build (apps/mobile)** — Scripts and docs to build an Android APK: `android.package` in app.json; `prebuild`, `build:apk:debug`, `build:apk:release` in apps/mobile; Nx target `build-apk` and root script `npm run mobile:apk` for local debug APK; `eas.json` with preview profile for EAS APK builds. See [apps/mobile/README.md](../apps/mobile/README.md#build-apk).
- **Demo images for web frontend (apps/web)** — Realistic demo images across landing, search, and property detail. Central `src/lib/demo-images.ts` with Unsplash URLs for cities (Mumbai, Bangalore, Delhi NCR, Hyderabad, Pune, Chennai) and properties (cover + gallery per listing). Next.js `images.remotePatterns` for Unsplash and Pexels. Property API extended with optional `coverImage` and `galleryImages`; mock data wired to demo images. Landing: city cards and “Properties You’ll Love” use real images; search grid and property detail gallery use `PropertyImage` with gradient/emoji fallback when no image. See `apps/web/README.md`.
- **Claude and enhanced thinking for agent (apps/api)** — Agent now supports Claude (Anthropic) via `AGENT_PROVIDER=anthropic`; `ANTHROPIC_API_KEY`, `AGENT_ANTHROPIC_MODEL`. Optional Claude extended thinking with `AGENT_THINKING_BUDGET_TOKENS`. Domain expert system prompt (Indian real estate: BHK, Cr, lakh, RERA, reasoning guidelines). Optional plan-first step (`AGENT_PLAN_FIRST`). Richer tool descriptions and new `compare_properties` tool (2–5 IDs). See [apps/api/src/modules/agent/MODULE_DOC.md](../apps/api/src/modules/agent/MODULE_DOC.md).
- **Enterprise LangChain AI Agent (apps/api)** — New `agent` module: single LangChain/LangGraph-style orchestrator with eight tools (search_properties, get_property, score_property, get_neighbourhood_score, get_price_forecast, check_rera, analyze_document, get_negotiation_advice). GraphQL: `askAgent(input: AskAgentInput!): AskAgentResult!` and `scoreProperty(propertyId: ID!): Property`. Env: `OPENAI_API_KEY`, `AGENT_MODEL`, `AGENT_MAX_STEPS`, `AGENT_QUEUE_ENABLED`, `REDIS_URL`. Tools use PropertyService for search/get/update; neighbourhood, forecast, RERA, document, negotiation return placeholders for future integrations. Unit tests for orchestrator and tools. See [apps/api/src/modules/agent/MODULE_DOC.md](../apps/api/src/modules/agent/MODULE_DOC.md).
- **Theme contrast and alignment polish (apps/web)** — Semantic tokens: `--heading`, `--btn-primary-text`, `--nav-bg`, `--ann-bar-bg`, `--hover-bg`, `--outline-stroke` in :root and .light. Header and announcement bar use theme-aware backgrounds; all headings/titles use `--heading`; primary buttons use `--btn-primary-text`. Light overrides for nav shadow, card hover shadow, map grid bg. Body line-height 1.6; nav min-height for tap targets. Shared tokens: `heading` and `btnPrimaryText` in libs/shared theme.ts.
- **next-themes and light theme (apps/web)** — Installed next-themes; added ThemeProvider (class-based, default dark, storageKey `urbannest-theme`); matching light theme via `.light` CSS variable overrides in globals.css (same token names, light palette); theme toggle in Nav (sun/moon, aria-label). Optional: `colorsLight` and `themeLight` in libs/shared tokens for JS access.
- **Enterprise API hardening (apps/api)** — Config module with Joi env validation; health module (Terminus, DB check); LoggingInterceptor and TimeoutInterceptor; ThrottlerGuard, AuthGuard placeholder, Helmet, explicit CORS; repository layer for property; strict feature layout (resolvers/, services/, dtos/, repository/, __tests__); LoggerService with entry/exit debug logs; GraphQL formatError and error codes (common/errors/errors.constants.ts); initial DB migration (CreateProperty). Run: `npm run dev:api`, `npm run build:api`.
- **NestJS GraphQL backend (apps/api)** — NestJS API with GraphQL (Apollo, code-first), TypeORM (Postgres), and property module. Queries: `properties(filter?)`, `property(id)`; mutations: `createProperty`, `updateProperty`, `deleteProperty`. Shared: Pino logger, `AppError`/domain errors, requestId middleware, global ValidationPipe. Run: `npm run dev:api`, `npm run build:api`. GraphQL playground at `http://localhost:3333/graphql`. Env: `DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASSWORD`, `DB_NAME`. See [apps/api/README.md](../apps/api/README.md).
- **Native mobile app (apps/mobile)** — Expo (React Native) app with NativeWind (Tailwind) and TypeScript. Mirrors web flows: Landing, Search, Property detail, Post property, More (About, EMI Calculator, Legal Checker, Neighbourhood, Price Forecast). Design tokens aligned with web. Run: `npm run mobile`, `npm run mobile:ios`, `npm run mobile:android`. See [apps/mobile/README.md](../apps/mobile/README.md).
- **libs/shared** — Shared design tokens (colors, radius) and types (nav links, property card) for web and mobile consistency. Path alias: `@property-app-ai/shared`.
- **Nx monorepo** — Repo converted to Nx workspace. Root `package.json` with workspaces `["apps/*"]`, `nx.json` for cache and task defaults, and `tsconfig.base.json` for shared TypeScript config.
- **apps/web** — Next.js app moved from `web/` to `apps/web`. Same UrbanNest.ai app (App Router, layout, routes); now managed by Nx with `@nx/next` plugin. Root scripts: `npm run dev`, `npm run build`, `npm run start`, `npm run lint` run via Nx.

### Changed

- **Cursor rules audit and subagent rule** — (1) All rule files now have frontmatter `description`. (2) master.mdc: added description, fixed "Afte" → "After", added monorepo layout line (apps/api, apps/*, libs/shared). (3) error_handling.mdc: description and domain errors generalized for Property-App (e.g. PropertyNotFoundError, ValidationError). (4) manual_actions.mdc: description, duplicate block removed. (5) header.template.mdc: "in detailed" → detailed template; author aligned to BharatERP; @file/@module/@description. (6) api_rules.mdc: clearer description; removed non-standard type. (7) MODULE_DOC.mdc and module_template.mdc: clearer descriptions. (8) subagents.mdc: "Always prefer subagents" section and workflow order (designer → architect → plan-reviewer → backend/frontend-engineer → tester → documenter); agent must invoke subagent when task matches instead of implementing in main agent.
- Next.js app location: `web/` → `apps/web/`.
- `apps/web/tsconfig.json` now extends `../../tsconfig.base.json`; app path alias `@/*` unchanged.
- Build/serve/lint executed from repo root using Nx (with caching).

### Technical

- Nx 21 + `@nx/next` plugin. Project `web` discovered from `apps/web`; targets inferred from package scripts and Next plugin. Second `nx run web:build` uses cache.
