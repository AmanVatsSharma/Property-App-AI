{
  "name": "property-app-ai",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "nx run web:dev",
    "build": "nx run web:build",
    "start": "nx run web:start",
    "lint": "nx run-many -t lint"
  },
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "nx": "^21.0.0",
    "@nx/next": "^21.0.0"
  }
}
